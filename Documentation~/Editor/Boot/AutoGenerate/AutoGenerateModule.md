# AutoGenerateModule.cs

An extension for the Editor.

Renders settings for `SettingsProvider`; used in `ProjectContextSettings`.

Responsible for editing the auto-generate settings for `SceneContext` when entering PlayMode.